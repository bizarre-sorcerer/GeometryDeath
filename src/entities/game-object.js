export class GameObject{
  constructor (x, y, dx, dy,
    thickness="2", color="#ffffff"){
    this.x = x
    this.y = y
    this.dx = dx
    this.dy = dy
    this.color = color
    this.thickness = thickness
    }
}